---
title: Schedule | Steel City Ruby Conf 2014 | August 15-16
---

- schedule = YAML.parse_file("schedule.yaml").to_ruby
%script(src='assets/js/schedule.js')
%section#hero
  %a{href: "/"}
    %img{src: 'assets/img/scrc14_hero_logo@2x.png', style: 'width:70%;margin-bottom:20px;'}
  %h2 Schedule of Events

  = partial "nav"

  #schedule
    .row
      .date-select
        %a.day-select{href: '#friday'}Friday
        %a.day-select{href: '#saturday'}Saturday

      -schedule.each do |day|
        - unless day['events'].nil?
          %div{ :id => day['day'], :class=> 'day' }
            %h2= day['day']
            -day['events'].each do |event|
              .event.large-12.columns
                %h3= event['time']
                %h4= event['name']
                .by-line
                  %img{"data-src" => "/assets/img/thumbs/#{event['img']}", "data-webp" => "/assets/img/thumbs/#{event['img'].match(/(.*)\./)[1]}.webp"}
                  %span= event['speaker']
                .desc
                  %p= event['desc']
      .scroll-to-top
